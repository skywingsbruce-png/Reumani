# 安装前请务必保持 numpy<2（见下），否则 scanpy/numba/statsmodels 会崩。
# === 核心 agent ===
langchain==1.3.1
langchain-anthropic==1.4.3
langchain-openai==1.3.3
langgraph==1.2.0
PyPDF2==3.0.1
requests==2.34.2
streamlit==1.58.0
python-dotenv==1.2.2
PyYAML

# === 数据分析 / 作图（注意 numpy 必须 <2，否则 scanpy/numba/statsmodels 会崩） ===
numpy<2
scipy<1.14
anndata<0.11
pandas
matplotlib
openpyxl
scikit-learn

# === SSc 专科技能依赖 ===
statsmodels        # 回归
lifelines          # 生存分析
gseapy             # 通路富集（需联网）
GEOparse           # 下载 GEO 数据集（需联网）
scanpy             # 单细胞
leidenalg          # 单细胞 Leiden 聚类
igraph

# === 混合检索（步骤1-5） ===
rank-bm25              # BM25 关键词召回
sentence-transformers  # dense 向量索引 + cross-encoder 重排序
# torch 由 sentence-transformers 拉起；CPU 版即可，无需 GPU

