# API mode (opt-in). Loaded by `vite --mode api` (npm run dev:api). Default mode stays mock.
VITE_REUMANI_DATA_SOURCE=api
VITE_REUMANI_API_BASE=http://127.0.0.1:8799
# 1 → 后端运行接入真实确定性组件的链路（离线冻结真实公开记录）；0/缺省 → fake demo。
VITE_REUMANI_DEMO_REAL=1
